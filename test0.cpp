#include<iostream>
#include <vector>
using namespace std;
int main_00(){
	long cityCount = 0, roadCount = 0;
	cin>>cityCount;
	cin >> roadCount;
	vector<int> sets;
	for(int roadNum = 0 ; roadNum < roadCount ; roadNum++){
		//Input the city number
		int cityA, cityB;
		cin >> cityA;
		cin >> cityB;

		//Construct Union-Find set
		

	}
	// result = cityCount - max(sets)
	return 0;
}